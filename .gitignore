__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/
dist/
build/
*.conf
/var/

# Scratch files people paste credential-bearing commands into - this repo is public,
# so these should never be one `git add -A` away from landing in history.
temp.txt
scratch.txt
*.secret
secrets.txt
